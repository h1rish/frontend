ID: my-frontend-app
_schema-version: "3.2"
version: 1.0.0
modules:
  - name: frontend
    type: html5
    path: build
    parameters:
      buildpack: staticfile_buildpack
      disk-quota: 256M
      memory: 256M
